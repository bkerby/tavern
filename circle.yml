machine:
  node:
    version: 11.13.0
  post:
    - npm install -g npm@3.x.x
deploy-prod:
    steps:
      - run:
          name: Deploy if tests pass and branch is Master
          command: npm run deploy