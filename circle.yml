machine:
  node:
    version: 11.13.0
  post:
    - npm install -g npm@3.x.x
deployment:
  production:
    branch: master
    commands:
      - firebase deploy --token=1/ZM8SV3sNVgM-nKsU5EmdFJO9uj3WFN5ZE-WmzzTgxVM --non-interactive
# $FIREBASE_TOKEN firebase login:ci